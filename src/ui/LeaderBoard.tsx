// Updating return types to conform with JSX.Element
const Scores = (): JSX.Element => {
    return <div>Scores here</div>;
};

const Score = (): JSX.Element => {
    return <div>Score here</div>;
};

//... other code not shown for brevity.